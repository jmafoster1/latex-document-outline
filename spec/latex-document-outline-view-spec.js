'use babel';

import LatexDocumentOutlineView from '../lib/latex-document-outline-view';

describe('LatexDocumentOutlineView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
